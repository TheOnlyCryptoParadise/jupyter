## Jupyter lab for cryptoparadise dashboard

### Steps to run:
 1. launch localstack by executing `docker-compose up` inside localstack directory from infra git repo.
 2. execute `docker-compose up` to start jupyter lab.