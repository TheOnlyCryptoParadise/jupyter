version: "3.9"
services:
  jupyter:
    image: lewelyn/cryptoparadise-jupyter
    networks:
      - localstack-net
    ports:
      - "8888:8888"
    environment:
      JUPYTER_ENABLE_LAB: "yes"
      FRONT_DNS_NAME: "localhost"
networks:
  localstack-net:
    external: true
    name: localstack-net
